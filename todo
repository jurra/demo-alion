- Upload to github
- Readme.md
- package.json description

- Change text placeholder Message form

- variable widths for mobile, vs desktop:
    - use bulma classes
        - Put lion background in main.scss and use an if mobile statement to remove the lion
        - Add input placeholder for other browsers


- Child component, slider of detailed employee info
    - Get state of position in array of employees.json 
    - active property to point to index in object array
    - slide on phone, with finger

- Router for pages per employee
  - Start with slider component

- Navbar component



--------- First Iteration Saturday/Sunday --------------------

- Load json in vue main app component
  -  Get state of position in array of employees.json 
  - x Pass only an object to child component, an object per new employee card created
  - x Pass json file to child component
  - x Make json file
  
- Styles: Get/Recreate Alion styles
  - Colors
  - x Font
  - Spacing between cards


- Child component employeecards
  -  x Render 3 cards per row 
    - x How to render 3 cards per row when we have 9, multi-line columns
    - ... read more button (animated)

- Form validation for contact
  - Set v-model

  - x Create row and column per card if number of cards is higher than n ammount of columns then create a new row and new columns per card
  - Add hover to styles
    - x On hover scale card 
    - Change color of name animation, as a keyframe

- Child component, slider of detailed employee info 
    - active property to point to index in object array
    - slide on phone, with finger

- Router for pages per employee
  - Start with slider component

- Navbar component

